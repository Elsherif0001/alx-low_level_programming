#include<stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 **/
int main(void)
{
	int o;

	for (o = 'a'; o <= 'z'; o++)
		putchar(o);
	putchar('\n');
	return (0);
}
